# docker pull nginx
# -d фоновый режим
# -p порт
# --restart возможность самостоятельной перезагрузки
# docker container rm "id container" - удалить ненужный процесс
# http://192.168.99.101:5000
docker run -d -p 5000:80 --name overlord --restart=always nginx